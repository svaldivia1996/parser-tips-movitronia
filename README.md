# parser-tips-movitronia
Parser de ejericicios de tablas excel
### Installation

requires [Node.js](https://nodejs.org/) v14+ to run.

Install the dependencies and run the code.

### Use
cambiar sourceFile
```sh
$ npm install
$ node index
```